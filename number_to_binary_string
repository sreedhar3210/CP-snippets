//converts given number into binary string

string n_to_bs(ll x)
{
    string s="";
    while(x>0){
        if(x&1)     s.push_back('1');
        else        s.push_back('0');
        x=x>>1;
    }
    reverse(s.begin(),s.end());
    return s;
}
